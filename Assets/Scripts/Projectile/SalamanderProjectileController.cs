using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SalamanderProjectileController : ProjectileController
{
    // Start is called before the first frame update
    void Start()
    {
        
        speed = 500;
        weight = 10;
        obeysGravity = true;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
